let animText = document.querySelectorAll('.header__content');

if(animText.length > 0){
    function textScroll(params) {
        for (let index = 0; index < animText.length; index++){
            const animItem = animText[index];
            const animItemHeight
        }
        console.log(text);
    }
} 